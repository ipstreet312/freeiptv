name: lib9w action

on:
  schedule:
    - cron: '15 */2 * * *'
  
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - name: config
        run: |
          git config --global user.email "<>"
          git config --global user.name "lib9w bot"
      
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install requests beautifulsoup4
      
      - name: updating lib9w
        run: ressources/extr/py/lib9w.py > ressources/extr/py/lib9w.m3u8
        
      - name: git add
        run: |
          git add -A
          ls -la 
          
      - name: commit & push
        run: |
          git pull origin master
          git commit -m "lib9w updated"
          git push origin master
