name: k12b action

on:
  schedule:
    - cron: '15 */18 * * *'
  
  workflow_dispatch:

jobs:
  runscript:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v2
      
      - name: config
        run: |
          git config --global user.email "<>"
          git config --global user.name "k12b bot"
      
      - name: updating k12
        run: sh ressources/keshetil/py/k12b.sh
        
      - name: git add
        run: |
          git add -A
          ls -la 
          
      - name: commit & push
        run: |
          git commit -m "k12b updated"
          git push
