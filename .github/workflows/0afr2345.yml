name: 0afr2345 action

on:
  schedule:
    - cron: '01 */4 * * *'
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
      - uses: actions/checkout@v2

      - name: config
        run: |
          git config --global user.email "<>"
          git config --global user.name "0afr2345 bot"
      
      - name: updating 0afr2345
        run: sh ${GITHUB_WORKSPACE}/ressources/frtv/py/0afr2345.sh
        
      - name: git add
        run: |
          git add -A
          ls -la 
          
      - name: commit & push
        run: |
          git commit -m "0afr2345 updated"
          git push
