name: 1fr345 action

on:
  schedule:
    - cron: '* */4 * * *'
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
      - uses: actions/checkout@v2

      - name: config
        run: |
          git config --global user.email "<>"
          git config --global user.name "1fr345 bot"
      
      - name: updating 1fr345
        run: sh ${GITHUB_WORKSPACE}/ressources/frtv/py/0a1fr345.sh
        
      - name: git add
        run: |
          git add -A
          ls -la 
          
      - name: commit & push
        run: |
          git commit -m "1fr345 updated"
          git push
